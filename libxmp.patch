--- libxmp-4.7.1-orig/include/xmp.h
+++ libxmp-4.7.1/include/xmp.h
@@ -67,6 +67,7 @@
 #define XMP_FORMAT_UNSIGNED	(1 << 1) /* Mix to unsigned samples */
 #define XMP_FORMAT_MONO		(1 << 2) /* Mix to mono instead of stereo */
 #define XMP_FORMAT_32BIT	(1 << 3) /* Mix to 32-bit int instead of 16 */
+#define XMP_FORMAT_BYTESWAP	(1 << 4) /* Mix to byte-swapped samples (only applicable for 16-bit mixing) */
 
 /* player parameters */
 #define XMP_PLAYER_AMP		0	/* Amplification factor */
--- libxmp-4.7.1-orig/src/mixer.c
+++ libxmp-4.7.1/src/mixer.c
@@ -76,39 +76,42 @@
 	int smp;
 	int shift = DOWNMIX_SHIFT + 8 - amp;
 
-	for (; num--; src++, dest++) {
-		smp = *src >> shift;
+	while (num--) {
+		smp = *src++ >> shift;
 		if (smp > LIM8_HI) {
-			*dest = LIM8_HI;
+			smp = LIM8_HI;
 		} else if (smp < LIM8_LO) {
-			*dest = LIM8_LO;
-		} else {
-			*dest = smp;
+			smp = LIM8_LO;
 		}
 
-		if (offs) *dest = (int8)((uint8)*dest + offs);
+		if (offs) smp = (int8)((uint8)smp + offs);
+
+		*dest++ = smp;
 	}
 }
 
 
 /* Downmix 32bit samples to 16bit, signed or unsigned, mono or stereo output */
 static void downmix_int_16bit(int16 *LIBXMP_RESTRICT dest,
-			      const int32 *src, int num, int amp, unsigned offs)
+			      const int32 *src, int num, int amp, unsigned offs, int byteswap)
 {
 	int smp;
 	int shift = DOWNMIX_SHIFT - amp;
 
-	for (; num--; src++, dest++) {
-		smp = *src >> shift;
+	while (num--) {
+		smp = *src++ >> shift;
 		if (smp > LIM16_HI) {
-			*dest = LIM16_HI;
+			smp = LIM16_HI;
 		} else if (smp < LIM16_LO) {
-			*dest = LIM16_LO;
-		} else {
-			*dest = smp;
+			smp = LIM16_LO;
 		}
 
-		if (offs) *dest = (int16)((uint16)*dest + offs);
+		if (offs) smp = (int16)((uint16)smp + offs);
+
+		if (byteswap)
+			*dest++ = __builtin_bswap16(smp);
+		else
+			*dest++ = smp;
 	}
 }
 
@@ -777,7 +780,8 @@
 				s->format & XMP_FORMAT_UNSIGNED ? 0x80000000u : 0u);
 	} else if (~s->format & XMP_FORMAT_8BIT) {
 		downmix_int_16bit((int16 *)s->buffer, s->buf32, size, s->amplify,
-				s->format & XMP_FORMAT_UNSIGNED ? 0x8000u : 0u);
+				s->format & XMP_FORMAT_UNSIGNED ? 0x8000u : 0u,
+				s->format & XMP_FORMAT_BYTESWAP);
 	} else {
 		downmix_int_8bit((int8 *)s->buffer, s->buf32, size, s->amplify,
 				s->format & XMP_FORMAT_UNSIGNED ? 0x80u : 0u);
